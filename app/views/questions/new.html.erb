

<div class="editor">
	<%= form_for @question do |f| %>
		<%= f.label :title %>
		<%= f.text_field :title, size: 135 %>
		<%= f.label :question %>
		<%= f.text_area :content, :class => "tinymce", :rows => 20, :cols => 98 %>
		<%= f.submit %>
	<% end %>
</div>